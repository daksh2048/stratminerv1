engine:
  timeframes: ["5m", "15m", "30m", "1h"]
  symbols: ["SPY", "NVDA", "GLD"]
  exchange: "yahoo"

  paper_balance: 10000.0
  risk_per_trade: 0.005
  slippage_bps: 1.5

  # your requirement:
  max_loss_pct: 0.03

  # Phase 1 safety (HIGHLY recommended):
  max_daily_loss_pct: 0.03
  max_positions: 3

logging:
  trades_csv: "trades.csv"
